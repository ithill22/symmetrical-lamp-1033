<h1>Items</h1>
<br>
<section id="items">
  <ul>
    <% @items.each do |item| %>
      <li><%= item.name %><br> 
      <%= "Price: #{item.price}" %><br> 
      <%= "Supermarket: #{item.supermarket_name}" %><br>
      <%= "Times Purchased: #{item.customer_count}" %>
      </li>
    <% end %>
  </ul>