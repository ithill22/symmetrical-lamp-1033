<h1>Items</h1>
<br>
<section id="items">
  <ul>
    <% @items.each do |item| %>
      <li><%= item.name %><br> 
      <%= "Price: #{item.price}" %><br> 
      <%= "Supermarket: #{item.supermarket.name}" %><br>
      <%= "Times Purchased: #{item.customers.count}" %>
      </li>
    <% end %>
  </ul>